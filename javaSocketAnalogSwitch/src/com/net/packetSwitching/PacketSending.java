package com.net.packetSwitching.sender;

import com.net.packetSwitching.Packet;

/**
 * 分组发送类
 * 主要功能：
 * 1. 接收发送方发送的数据包
 * 2. 将数据包发送给交换机
 * 3. 接收交换机发送的确认消息
 * 4. 将确认消息发送给发送方
 * 5. 重复1-4，直到接收到最后一个分组
 * */
public class PacketSending {

    // 发送数据包
    public void sendPacket(Packet packet) {

    }


}
